{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && copyfiles -u 1 Build/**/* dist/Build",
    "preview": "vite preview",
    "tunnel": "cloudflared tunnel --url http://localhost:5173/"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^1.2.0",
    "cloudflared": "^0.5.3"
  }
}
